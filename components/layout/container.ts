/**
 * The one horizontal gutter used by every page-level block: header, footer
 * and every section. Keeping it in one place is what stops the left edge
 * drifting between pages.
 */
export const pageGutter = "px-6 sm:px-10 lg:px-12 xl:px-20";
